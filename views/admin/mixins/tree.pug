mixin tree(items, level = 1)
  each item in items
    - const prefix = Array(level + 1).join("--- ")
    option(value=item._id) #{prefix}#{item.title}
        if item.children && item.children.length > 0
            +tree(item.children, level + 1)